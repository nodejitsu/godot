/*
 * tcp.js: Top-level include for the `tcp` module responsible for creating TCP servers / clients.
 *
 * (C) 2012, Nodejitsu Inc.
 *
 */
 
var net = require('net');

exports.createServer = function () {
  
};

exports.createClient = function () {
  
};